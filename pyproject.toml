[project]
name = "de25-lab2-azure-antone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "frontend",
    "backend",
]

[dependency-groups]
dev = [
    "kagglehub>=1.0.2",
]
